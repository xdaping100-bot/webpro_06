```mermaid
stateDiagram-v2
    [*] --> 一覧表示
    一覧表示 --> 詳細表示: リンク押下
    一覧表示 --> 新規登録フォーム: 追加ボタン
    新規登録フォーム --> 一覧表示: 登録(POST) -> リダイレクト
    詳細表示 --> 編集フォーム: 編集ボタン
    詳細表示 --> 一覧表示: 戻るリンク / 削除後
    編集フォーム --> 一覧表示: 更新(POST) -> リダイレクト
    詳細表示 --> 削除処理: 削除ボタン
    削除処理 --> 一覧表示: 削除実行(GET) -> リダイレクト
```